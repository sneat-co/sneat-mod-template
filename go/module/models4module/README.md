# models4module

Models persisted through `dal-go/dalgo` (e.g. database records).

`thing.go` defines `Thing`, a minimal example record with one required field,
used by `facade4module` to demonstrate this module's data-access pattern.
